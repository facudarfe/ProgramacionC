#include <stdio.h>
#define PI 3.1415
#define MOSTRAR(x) printf("%s%, X);

int main(int argc, char *argv[]) {
	int r = 5;
	printf("El area del circulo es: %f", PI*r*r);
	MOSTRAR("Hola mundo");
	return 0;
}

